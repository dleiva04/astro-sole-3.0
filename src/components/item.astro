---
const { key, id, name, price, isOnSale, isRecommendation } = Astro.props;
---

<div>
    <a href=`/producto/${id}`>
        <div class="relative">
            <div
                class="absolute top-0 right-0 pr-3 pt-3"
                onclick="
                console.log('click')
                "
            >
            </div>
            {
                isOnSale ? (
                    <span class="absolute top-0 right-2 md:right-0 inline-flex items-center py-0.5 px-1.5 rounded-full text-xs font-medium transform -translate-y-1/2 translate-x-1/2 bg-rose-500 text-white">
                        -25%
                    </span>
                ) : (
                    ""
                )
            }
        </div>
        <img
            src="/image.jpg"
            class={`transition duration-200 ease-linear ${
                isRecommendation ? "h-[200px] md:h-[400px] " : ""
            })`}
            alt=""
            onmouseover={`if (window.matchMedia('(min-width: 768px)').matches) this.src='/image2.jpg'`}
            onmouseout={`if (window.matchMedia('(min-width: 768px)').matches)this.src='/image.jpg'`}
        />
    </a>
    <div class={`py-2 mt-2 ${isRecommendation ? "w-28 md:w-full" : ""} `}>
        <a href=`/producto/${id}` class="hover:underline">
            <p
                class={`font-extrabold ${
                    isRecommendation ? "font-sm " : "font-lg "
                }`}
            >
                {name}
            </p>
        </a>
        <p
            class={`py-1 ${
                isRecommendation ? "font-sm " : "font-lg "
            } text-gray-600`}
        >
            {price}
        </p>
    </div>
</div>
